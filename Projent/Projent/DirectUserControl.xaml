<UserControl x:Class="Projent.DirectUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Projent"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="50"
             d:DesignWidth="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Width="40"
                Height="40"
                Margin="0 0 10 0"
                Grid.Column="0" CornerRadius="40 40 40 40">
            <Image Source="/Assets/Icons/user.png"
                   x:Name="img_profilePic" />
        </Border>

        <StackPanel Grid.Column="1">
            <TextBlock Text="LilyKi"
                       FontWeight="Bold"
                       Foreground="#ff508E00"
                       x:Name="lbl_username" />
            <TextBlock Text="lilyki@gmail.com"
                       x:Name="lbl_email" />
        </StackPanel>

    </Grid>
</UserControl>
